From 6649db8d71c2c3ebe54c5a821b8d22f8fb7bf226 Mon Sep 17 00:00:00 2001
From: Boris Belyaev <bbor1974@gmail.com>
Date: Tue, 23 Dec 2025 17:02:15 +0400
Subject: [PATCH 3/5] init: modules in /system/lib/modules/

Change-Id: If31b0fe1709b71108c5eaa7e28f730ae31cfbf3f
---
 init/builtins.cpp | 8 ++++----
 1 file changed, 4 insertions(+), 4 deletions(-)

diff --git a/init/builtins.cpp b/init/builtins.cpp
index 16cf36b..9a6499c 100755
--- a/init/builtins.cpp
+++ b/init/builtins.cpp
@@ -1302,7 +1302,7 @@ ERROR("-----------rogge---------------do_insmod_modules");
         if (back_name_len > 0)
         {
             memset(str, 0, 128);
-            sprintf(str, "/system/vendor/modules/%s.ko", back_name);
+            sprintf(str, "/system/lib/modules/%s.ko", back_name);
             ret = insmod(str, options);
             if(ret < 0)
             {
@@ -1316,7 +1316,7 @@ ERROR("-----------rogge---------------do_insmod_modules");
         if (front_name_len > 0)
         {
           memset(str, 0, 128);
-            sprintf(str, "/system/vendor/modules/%s.ko", front_name);
+            sprintf(str, "/system/lib/modules/%s.ko", front_name);
             ret = insmod(str, options);
             if(ret < 0)
             {
@@ -1330,7 +1330,7 @@ ERROR("-----------rogge---------------do_insmod_modules");
         if (back_node_len > 0)
         {
             memset(str, 0, 128);
-            sprintf(str, "/system/vendor/modules/%s.ko", back_node);
+            sprintf(str, "/system/lib/modules/%s.ko", back_node);
             ret = insmod(str, options);
             if(ret < 0)
             {
@@ -1344,7 +1344,7 @@ ERROR("-----------rogge---------------do_insmod_modules");
         if (front_node_len > 0)
         {
           memset(str, 0, 128);
-            sprintf(str, "/system/vendor/modules/%s.ko", front_node);
+            sprintf(str, "/system/lib/modules/%s.ko", front_node);
             ret = insmod(str, options);
             if(ret < 0)
             {
-- 
2.43.0

