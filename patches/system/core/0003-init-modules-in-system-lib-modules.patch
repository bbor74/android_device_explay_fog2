From ae3dd27587ee0315a4ddd26d3efab29ee7bf43ef Mon Sep 17 00:00:00 2001
From: Boris Belyaev <bbor1974@gmail.com>
Date: Tue, 23 Dec 2025 17:02:15 +0400
Subject: [PATCH 3/4] init: modules in /system/lib/modules/

Change-Id: If31b0fe1709b71108c5eaa7e28f730ae31cfbf3f
---
 init/builtins.cpp | 8 ++++----
 1 file changed, 4 insertions(+), 4 deletions(-)

diff --git a/init/builtins.cpp b/init/builtins.cpp
index 76e2c865d..7f2b83791 100755
--- a/init/builtins.cpp
+++ b/init/builtins.cpp
@@ -1287,7 +1287,7 @@ ERROR("-----------rogge---------------do_insmod_modules");
         if (back_name_len > 0)
         {
             memset(str, 0, 128);
-            sprintf(str, "/system/vendor/modules/%s.ko", back_name);
+            sprintf(str, "/system/lib/modules/%s.ko", back_name);
             ret = insmod(str, options);
             if(ret < 0)
             {
@@ -1301,7 +1301,7 @@ ERROR("-----------rogge---------------do_insmod_modules");
         if (front_name_len > 0)
         {
           memset(str, 0, 128);
-            sprintf(str, "/system/vendor/modules/%s.ko", front_name);
+            sprintf(str, "/system/lib/modules/%s.ko", front_name);
             ret = insmod(str, options);
             if(ret < 0)
             {
@@ -1315,7 +1315,7 @@ ERROR("-----------rogge---------------do_insmod_modules");
         if (back_node_len > 0)
         {
             memset(str, 0, 128);
-            sprintf(str, "/system/vendor/modules/%s.ko", back_node);
+            sprintf(str, "/system/lib/modules/%s.ko", back_node);
             ret = insmod(str, options);
             if(ret < 0)
             {
@@ -1329,7 +1329,7 @@ ERROR("-----------rogge---------------do_insmod_modules");
         if (front_node_len > 0)
         {
           memset(str, 0, 128);
-            sprintf(str, "/system/vendor/modules/%s.ko", front_node);
+            sprintf(str, "/system/lib/modules/%s.ko", front_node);
             ret = insmod(str, options);
             if(ret < 0)
             {
-- 
2.17.1

